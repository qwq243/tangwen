# ZCode 工作区文件搜索忽略规则（.zcodeignore）
# 语法与 .gitignore 一致，只影响 ZCode 的 @ 文件候选 / Command Center / 文件树搜索，
# 不影响文件树浏览、上传或 Agent 文件访问。
# 修改 .gitignore 不会自动同步到本文件；可在设置页「从 .gitignore 同步」。

# 汤问 · 海龟汤 —— 仓库只收「源码 + 语料 + 封面 + 文档」。
# 判据跟 pack.py 那份「运行期必需」清单对齐：装不进部署包的东西，也不进版本库
# （文档与截图是例外，它们服务的是读代码的人，不是运行期）。

# 运行期状态：排行榜 / 结案人数 / 埋点，server.py 每次都读改写（DATA_DIR 可另指）
/data/

# 部署包与覆盖包：跑 pack.py / wrangler 就有，每次都是新的
/dist/

# 已停用的服务端 ASR 模型：asr.py 会按需重下（约 148MB），重下了也不要入库
/models/

# 临时产物：探针截图、假数据、一次性的排查脚本
/tmp/

# Python 字节码与虚拟环境
__pycache__/
*.py[cod]
.venv/
venv/

# Node 依赖与 Cloudflare 本地状态（npx wrangler 会重建）
node_modules/
.wrangler/

# 智能体 / 编辑器的本地状态
.workbuddy/

# 密钥与日志。判题密钥走环境变量或 Pages Secret，绝不写进仓库。
# .env.example 是模板，要入库 —— 它必须排在 .env.* 那条通配规则后面才生效。
.env
.env.*
!.env.example
*.log

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
__pycache__/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
